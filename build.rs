fn main() {
    // Placeholder for future Windows resource (icon, version info).
    // Add `resources/blackhole.rc` and uncomment below to embed.
    // #[cfg(windows)]
    // embed_resource::compile("resources/blackhole.rc", embed_resource::NONE);
}
